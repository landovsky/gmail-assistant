# TBA

## Selected stack
- Bun — runtime, test runner, package manager
- Hono — HTTP framework, REST API, webhooks
- Hono JSX — server-rendered debug/admin pages
- Drizzle — ORM, migrations, type-safe queries (SQLite + PostgreSQL)
- Drizzle Studio — database browser (replaces SQLAdmin)
- Zod — validation for API inputs, config, LLM responses
- Vercel AI SDK — LLM calls, tool-use agent loop, structured output
- googleapis — Gmail API client, OAuth, Pub/Sub watch
- BullMQ — job queue, retries, scheduled tasks (or SQLite-based if no Redis)
- Commander — CLI commands
- yaml — config file parsing
# Specific email addresses
style_overrides:
  "teacher@school.cz": formal
  "partner@company.cz": business
  "friend@gmail.com": informal

# Domain patterns
domain_overrides:
  "*.gov.cz": formal
  "*.mfcr.cz": formal
  "*.edu.cz": formal

# Language overrides (when auto-detection isn't enough)
language_overrides:
  "english-client@abroad.com": en

# Blacklist — always classify as FYI, never draft a response.
# Supports glob patterns (* matches any characters).
blacklist:
  - "*@noreply.github.com"
  - "*@notifications.google.com"
